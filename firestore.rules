rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all for the prototype phase.
    // In production, this should be restricted by auth.uid and custom claims.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
